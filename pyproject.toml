[project]
name = "patient-memory"
version = "0.1.0"
description = "Patient Health Memory — graph-based reasoning over scattered medical records with Cognee (hackathon demo)"
requires-python = ">=3.12"
dependencies = [
    "cognee>=1.2.2",
    "fastapi>=0.139.0",
    "uvicorn[standard]>=0.50.0",
]
